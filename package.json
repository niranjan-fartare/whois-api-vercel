{
  "name": "express",
  "version": "1.0.0",
  "description": "",
  "main": "api/index.ts", // Change this to .js after transpilation
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node api/index.ts",
    "build": "tsc" // Add a build step
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@vercel/postgres": "^0.7.2",
    "dotenv": "^16.4.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "typescript": "^5.0.0" // Add TypeScript as a dev dependency
  }
}
